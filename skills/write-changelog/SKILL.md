---
name: write-changelog
description: Use when the user wants to render a user-facing changelog from a GitHub milestone — phrases like "render changelog from milestone", "generate release notes", or "user-facing changelog for v0.2.0". Sources entries from the milestone's closed issues and their merging PRs, applies repo-specific style/voice when present, and writes Markdown to stdout (or inserts under `## [Unreleased]` in `CHANGELOG.md` with `--write`).
---

Follow the instructions in ./workflow.md.
